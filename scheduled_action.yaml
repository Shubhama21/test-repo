scheduledActionName: <+serviceVariables.scheduled_action_name>
serviceNamespace: ecs
schedule: "cron(0 8 * * ? *)" # 8 AM UTC
scalableDimension: "ecs:service:DesiredCount"
scalableTargetAction:
  minCapacity: 1
  maxCapacity: 10
